{
  "name": "nexpoint-website",
  "private": true,
  "scripts": {
    "check:js": "bash -c 'set -e; for f in assets/*.js hub/assets/*.js; do node --check \"$f\"; done; echo js-ok'",
    "check:html": "html-validate 'index.html' 'hub/**/*.html' 'printhub/*.html' 'millhub/*.html' 'opportunities/*.html' 'portal/*.html' 'admin/*.html'",
    "check:links": "node check/links.mjs",
    "check": "npm run check:js && npm run check:html && npm run check:links"
  },
  "devDependencies": {
    "html-validate": "^8.29.0"
  }
}
